# RoamBot 需求访谈与约束记录

本文件记录用户在需求访谈中的回答。后续 `SPEC.md`、`PLAN.md`、实现与验收均应以这些回答为约束。

## 已确认

- 项目名称：RoamBot。
- 项目类型：AI4SE 期末项目 B 类应用项目。
- Superpowers：已在当前会话接入，作为开发流程工具使用，不作为 RoamBot 运行时依赖。
- 已放弃核心功能：12306 查询、机票查询、强接 SunsetBot。
- 已保留方向：未来一周天气、风景偏好、多人多地汇合、推荐排序、LLM 解释。
- 时间约束：当前作业有约一个月打磨时间，不按“两天赶工版”收缩范围；后续设计直接面向可展示的最终版。
- 交付平台优先级：正式作业先实现响应式 Web 应用，满足线上 WebUI、Docker 分发与 CI 要求。登录、收藏、历史记录和分享按 Web 功能继续设计。微信小程序不进入当前验收范围；只有 Web 主项目全部完成且仍有余量时，才作为个人使用的独立扩展另行设计。
- Web 架构：采用 React + TypeScript + Vite 前端与 FastAPI 后端分离的源码结构。开发时前后端分别运行；生产环境将 React 构建产物与 FastAPI 打入同一个 Docker 镜像，通过同一域名提供页面和 `/api` 接口，避免跨域登录复杂度。前端使用 Vitest，后端使用 pytest，关键流程使用 Playwright。
- API 契约：公开 JSON API 使用 `/api/v1` 前缀；推荐与指定地点评估使用两个独立入口，内部共享天气、距离与评分服务。账户使用服务端会话和 `HttpOnly` Cookie，状态修改请求启用 CSRF 防护；浏览器不保存密码、API key、完整会话令牌或私人历史数据。
- 登录范围：采用本地用户名和密码账户，支持注册、登录和退出。用户名必须唯一，密码只保存安全哈希，登录后使用安全会话识别用户；不做邮箱验证、找回密码或第三方登录。
- 注册验证边界：V1 不收集手机号，不发送短信验证码，也不接入图片、滑块或云验证码服务；这些能力会增加供应商、审核、费用、隐私和防刷范围，待 Web 主项目全部验收后再独立评估。
- 游客访问范围：核心推荐和指定地点评估无需登录即可使用。收藏、历史记录、历史删除和创建/撤销分享链接必须登录；游客查询不写入个人历史。公开分享链接仍可匿名只读访问。
- 收藏范围：收藏保存“用户喜欢的地点”本身，不冻结当时的天气、评分或推荐理由。用户可添加、取消和查看收藏；重新打开收藏地点时，必须根据新的出发地、日期和实时天气重新评估。
- 历史记录范围：仅自动记录成功完成的推荐或指定地点评估。每条历史保存查询输入、生成时间和当时的结构化结果摘要，并明确标记为历史快照；非法或失败请求不记录。用户点击“重新查询”时使用当前可用天气重新计算，并生成一条新的历史记录。用户可删除单条历史或清空自己的全部历史；删除历史会同时使其关联分享链接失效，但不影响收藏地点。当前不做数据导出或账户注销。
- 分享范围：登录用户可为自己的历史快照生成不可猜测、可撤销的只读分享链接。接收者无需登录，可查看地点、日期、天气摘要、评分、推荐理由和生成时间；公开内容必须移除用户身份、详细出发地址和其他私人数据。不做社交动态、评论或微信专用分享接口。
- 数据持久化：使用 SQLite 保存账户、会话、收藏、历史和分享链接。开发与单元测试不依赖外部数据库服务器；Docker 运行时将 SQLite 文件放在挂载的数据卷中，容器重建后数据仍可保留。
- 分发方式：正式交付选择 Docker 容器镜像，并按作业要求提供单条构建、单条运行命令和公开镜像仓库。Docker 不是作业唯一允许的分发方式，但最适合 RoamBot 这种带服务端的 Web 应用。
- API 凭据安全：高德、QWeather 和 LLM key 使用带主密码的加密凭据库保存，不使用 `.env` 或普通环境变量保存真实 key。初始化、查看状态、更新、清除和紧急重置只能通过本机管理员终端执行；普通 WebUI 不提供凭据管理入口。正常清除需要当前主密码；忘记主密码后的重置需要 Docker 主机权限和不可逆确认，只重建凭据库，不删除 SQLite 用户数据。
- API 凭据申请时机：设计、骨架开发、评分实现和自动测试阶段均使用 mock，不要求真实凭据。只有 provider 适配器及 mock 测试通过、准备执行人工真实 API 冒烟测试时，才由用户申请并通过本机隐藏终端录入；不得在聊天中发送 key。
- 核心问题优先级：RoamBot 首先解决“个人提出天气、距离和风景偏好要求后，程序帮助找到合适地点并给出推荐选项”；多人多地汇合是额外功能，不作为第一主线。
- 多人功能约束：允许额外地址参与评分；多人公平性按“多个出发地到目的地的距离差异/方差”定义，方差越小越公平。平均距离和最大距离用于展示解释，不作为公平性主定义。
- 地理范围约束：最终目标是不预设城市范围，按用户输入的出发地和最大距离筛选候选地点；演示场景固定苏州为默认演示城市，用户不填写城市时也默认使用苏州。
- 风景类型：湖景、海景、古镇/历史街区、博物馆、公园/绿地/湿地、山地/徒步。苏州默认演示优先覆盖除海景外的类型；海景作为跨城市或较远距离扩展类型保留。
- 风景类型识别：候选地点的内部风景标签由后端确定性规则生成，主要依据高德返回的 POI `type` / `typecode`，再以地点名称关键词补充，并允许对少量知名地点配置人工修正。一个地点可以有多个标签；无法可靠识别时标记为未知，不让 LLM 猜测或决定分类。
- 日期区间与天气评分：用户输入一个未来可预测范围内的日期区间，例如 7.2-7.5。RoamBot 推荐的是这 n 天整体都适合的地点，而不是只挑某一天最适合的地点；前端仍需展示该地点在所选日期区间内每天各自的旅游适宜度。
- 日期区间综合天气分：采用 `每日适宜度平均分 × 70% + 最差一天适宜度 × 30%`，而不是单纯平均或只取最低分；结果限制在 0-100 分。
- 每日适宜度展示：每天的适配度应告知用户分数来源，并结合用户选择的风景需求解释为什么这一天适合或不适合。
- 多风景类型解释模式：用户可选择“尽量覆盖全部偏好”或“匹配任意偏好即可”。前者适合多日行程，系统需要考虑日期区间内是否尽量覆盖用户选择的多种风景；后者适合轻量推荐，只要地点匹配部分偏好即可。
- 多风景类型不做日程安排：即使选择“尽量覆盖全部偏好”，RoamBot 也不安排“第几天玩什么”的具体行程，只计算地点/结果对用户偏好的覆盖度，并说明哪些偏好被覆盖、哪些未覆盖。
- 结果展示边界：前端只展示地点卡片、距离、每日天气、分项评分、总体推荐度和推荐理由；不嵌入地图，不绘制路线，不提供导航或具体行程安排。高德仅作为后台地址解析、地点检索和距离估算的数据服务。
- WebUI V1 基线：首屏直接提供推荐工具，不制作营销落地页；顶部导航包含推荐、收藏、历史和账户入口。桌面端输入区与结果区左右排列，手机端上下排列；推荐/指定地点使用模式切换，结果使用地点卡片。该结构是第一版实现基线，开发阶段可在不破坏 API 语义和验收规则的前提下持续调整视觉与交互。
- 指定地点评估模式：用户可以直接输入想去的地点，例如“金鸡湖”，并给出游玩目标、日期区间、距离/多人信息；系统不返回推荐地点列表，而是返回该指定地点的推荐指数、每日旅游适宜度、原因与风险提示。
- 指定地点评估模式的防御性输入约束：用户填写目标地点后，不再填写风景类型；前端应隐藏或禁用风景类型输入，后端也应拒绝“目标地点 + 风景类型”的矛盾请求。指定地点的风景标签由系统根据地点信息识别，并用于解释该地点本身适合什么。
- 地图 API：主选高德地图 Web 服务 API，用于地址解析、POI/周边景点检索、地点分类和距离估算；百度地图作为备选调研对象，本地 mock 数据用于测试。产品不展示地图、路线或导航。
- POI 失败降级：高德 POI 检索失败时可使用仍在有效期内的同范围缓存，并明确提示缓存来源。内置苏州地点只允许在管理员显式开启的演示模式中使用，页面必须标注“演示数据”；正式模式不得静默使用内置数据，无有效缓存时应明确返回地点服务不可用。
- 天气 API：主选 QWeather（和风天气）每日天气预报 API，用于按地点经纬度查询未来日期天气；RoamBot 产品层仍默认限制未来 7 天，以保证预报参考价值。
- QWeather 接入：V1 使用账户专属 `*.qweatherapi.com` API Host 和官方仍支持的 `X-QW-Api-Key` Header 认证，不增加 JWT 签名复杂度；高德 GCJ-02 坐标按原值保留两位传入，并在真实苏州 smoke 中核对合理性。
- LLM API：采用 OpenAI-compatible provider 抽象，配置 `base_url`、`api_key`、`model`；默认演示模型使用学校额度平台的 DeepSeek V4 Flash。真实 LLM 仅在配置 key 后启用，测试与 CI 使用 mock/template，不消耗 token。
- 游玩时长：不依赖地图 API 直接给出推荐游玩时长；后续若展示该字段，应由系统根据地点类型、热度/推荐度和规则估算，并明确标注为估算值。
- API 成本控制：测试与 CI 不调用真实地图、天气或 LLM API；真实演示时先用本地候选与粗距离筛选，再只对少量候选调用高德距离测量和 QWeather，避免请求量随候选数量失控。
- 候选调用顺序：POI 粗距离过滤后按既有 provider 顺序最多取五项；逐项先查精确距离并执行硬过滤，只有通过的候选才查天气，精确超距不回填，避免无效 QWeather 调用。
- 缓存兼容性：缓存采用带 schema version 的确定性 SHA-256 key 和 Pydantic JSON envelope；严格 `now < expires_at`，不做 stale-if-error。来源事件使用每请求独立 trace，避免并发响应互相污染。
- Python 版本：本机开发与快速测试使用 Codex bundled Python 3.12.13，包兼容范围为 `>=3.12,<3.14`；最终 Docker 与 GitLab CI 固定使用 Python 3.13，避免为了本地开发额外安装系统解释器。
- 测试与交付：后端使用 pytest，前端使用 Vitest，关键流程使用 Playwright；真实 API 冒烟测试与自动测试分离并由管理员手动触发。Docker 使用单镜像、持久化数据卷和公开 registry，GitLab CI 至少提供 `unit-test` job 且不注入真实 key。
- 景区热度来源：高德未稳定提供统一的景点数字热度时，根据高德 POI 综合搜索排名生成基础分，并允许对少量知名地点使用可审查的本地修正。该指标在界面中明确标为“RoamBot 热度估算”，不能冒充高德官方评分。
- 评分确定性：距离分按所有出发地平均距离相对最大距离线性下降；公平性按距离总体标准差相对最大距离线性下降；热度按 `100-4×(provider_rank-1)` 计算。V1 本地热度修正表为空，所有分项与总分在函数出口限制到 0-100 并保留两位小数。
- 表单确定性：V1 默认推荐模式、苏州、50 km、明天单日、`any`；风景默认不选。权重分段以 5% 为步长只在相邻段间转移，人数从单人/多人切换时恢复对应默认，指定地点评估也可调整同一套权重。
- 推荐排序约束：
  - 风景类型匹配是硬约束，不满足用户选择风景类型的地点不进入常规推荐。
  - 距离在用户可接受范围内是硬约束，不满足最大距离要求的地点不进入常规推荐。
  - 用户可自定义权重，权重项包括天气适配、距离远近、多人距离方差（仅多人多地时出现）、景区热度/推荐度。
  - 权重调整方式：使用一个分段滑块条自动归一化。多人时一个条上三个滑块，形成天气适配、距离远近、多人距离方差、景区热度/推荐度四段；单人时一个条上两个滑块，形成天气适配、距离远近、景区热度/推荐度三段。
  - 默认单人排序权重：天气适配 40%，距离远近 30%，景区热度/推荐度 30%。
  - 默认多人排序权重：天气适配 40%，多人距离方差 40%，景区热度/推荐度 20%，距离远近 0%。
  - 若规定范围内无符合风景类型的检索结果，常规推荐窗口显示“规定范围内无检索结果”；下方扩展窗口展示“最近距离的符合风景要求景点”和“最热门的符合风景要求景点”。

## 已确认访谈结论

1. RoamBot 最核心想解决的问题是什么？已确认：个人天气/距离/风景推荐为主，多人多地汇合作为额外功能。
2. 主要服务什么地理范围？已确认：长期目标按用户输入最大距离决定范围；默认演示城市为苏州。
3. 保留哪些风景类型？已确认：湖景、海景、古镇/历史街区、博物馆、公园/绿地/湿地、山地/徒步。
4. 推荐排序中各因素优先级如何？已确认：风景类型和最大距离为硬约束；用户可调权重；默认单人天气 40%、距离 30%、热度 30%；默认多人天气 40%、方差 40%、热度 20%、距离 0%。
5. 多人汇合时怎样定义“公平”？已确认：看多个出发地到目的地的距离差异/方差，方差越小越公平。
6. 输入形式和结果展示要做到什么程度？已确认：结果以卡片展示地点、距离、每日天气、分项评分、总体推荐度和推荐理由；支持指定地点直接评估模式；指定地点模式下不允许用户填写风景类型；不展示地图、路线、导航或具体日程。
7. 地图、天气、LLM API 优先选择什么？已确认：地图主选高德地图 Web 服务 API；天气主选 QWeather（和风天气）；LLM 采用 OpenAI-compatible provider 抽象，默认演示模型为学校额度平台的 DeepSeek V4 Flash。
8. 哪些功能明确不做？已确认：不做地图展示、路线绘制、导航、具体日程、12306、机票、社交动态、邮箱体系或第三方登录；微信小程序仅作为 Web 全部验收后的独立可选扩展。
