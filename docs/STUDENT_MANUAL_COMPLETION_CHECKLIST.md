# RoamBot 暑期任务：学生本人完成清单

> 本清单只列必须由学生本人完成、确认或承担责任的事项。技术命令、事实整理和文字润色可以由 AI 辅助，但不能让 AI 替代个人判断、真实操作或反思正文。

## 一、必须由本人撰写

- [x] 用自己的经历和判断完成 `REFLECTION.md`，正文约 2342 字，符合 1500-2500 字要求。
- [x] 已删除写作提示，只保留正文、标题和 AI 辅助声明。
- [x] 已写出对 Brainstorming、机械审查、测试、部署和重做流程的个人判断。
- [x] 已使用 SPEC/PLAN 冷启动遗漏和最大距离 `0200` 回归测试作为亲历案例。
- [x] 文末已如实说明 Codex 与 Superpowers 的参与范围以及本文的整理方式。

作业明确禁止 AI 代写反思。AI 可以在你写完后指出语病、重复和结构问题，但不应替你生成可直接提交的正文。

## 二、必须由本人核对并承担责任

- [ ] 通读 `SPEC.md`、`PLAN.md`、`SPEC_PROCESS.md`、`AGENT_LOG.md`、`README.md`，确认其中的需求、决策、测试结果和时间记录真实，没有把“计划执行”写成“已经执行”。
- [ ] 确认你能当面解释：项目解决什么问题、为何不做地图/导航/行程安排、评分怎样计算、Mock 与 Live 有何区别、为何使用加密凭据库、Docker 数据卷保存什么。
- [ ] 亲自确认所有界面和功能达到你认可的程度；AI 的自动测试通过不能替代你的验收判断。
- [ ] 对课程要求与实际流程的偏离作出自己的解释。例如前期陌生 agent 和逐 task 审查明显过量，后来改为大里程碑集中检查；这应如实写进过程记录和反思，而不是隐藏。
- [x] 检查最终文档中的“游客能否直接查询”表述是否与最终实现一致：游客可以直接使用推荐和指定地点评价；收藏、历史、重新查询和个人分享管理等个人数据功能需要登录。该口径已与 `SPEC.md`、`README.md`、后端路由和游客端到端测试核对一致。

## 三、必须由本人执行的账户与凭据操作

- [ ] 保管高德、QWeather、学校 LLM 的真实 key、QWeather API Host 和 RoamBot 主密码；不要粘贴给任何 AI，也不要写入仓库、聊天、截图或普通配置文件。
- [x] 在 Railway 上创建账户、确认试用额度、配置 secret 和持久化存储；这些本人操作已经完成。
- [x] 公网部署使用 Live 模式，真实凭据已通过 Railway secret 录入；现有脱敏日志与页面检查未发现 key 回显。
- [ ] 若遗忘凭据库主密码，只能 reset 后重新录入并撤销旧 key；不要尝试把真实 key 交给 AI 排错。

## 四、最终人工验收

- [ ] 用最终构建版本在桌面端和手机宽度各完成一轮主流程：登录、推荐、指定地点评估、收藏/取消收藏、历史、重新查询、分享/撤销、删除历史。
- [ ] 使用浏览器无痕窗口打开分享链接，确认无需登录且不含用户名、详细出发地、Cookie 或凭据。
- [ ] 至少检查一个单日、一个多日、一个多人、一个跨城市、一个异常输入场景。
- [ ] 若最终代码仍修改了后端 provider 或推荐逻辑，再进行一次小样本 Live smoke；记录真实调用次数和结果，不需要反复消耗额度。
- [ ] 保存必要的脱敏截图或简短录屏作为演示证据；不得出现 API key、主密码、Cookie、完整个人地址或账户敏感信息。

## 五、线上部署与公开访问

- [x] 已选择并开通 Railway：支持 Docker、HTTPS、secret 和持久化数据卷。
- [x] SQLite 数据库和 `credentials.vault` 使用 `/data` 持久化目录；Railway 数据卷容量 500 MB。
- [x] 生产环境启用安全 Cookie，并已在同域 HTTPS 下验证登录、CSRF 与分享流程。
- [x] 公网 WebUI 为 `https://roambot-production.up.railway.app`，已写入 README。
- [ ] 从另一台设备或手机网络访问公网 URL；当前只确认本机浏览器通过公网域名访问，不把响应式模拟等同于跨设备验证。

## 六、仓库与课程提交

- [ ] 让 AI 辅助完成最终测试、Docker 构建、secret 扫描和文档事实更新后，由本人审阅 diff。
- [x] 正式 PR [#1](https://github.com/TheOwl060218/RoamBot/pull/1) 已由本人明确同意并合并：`feat/roambot-v1` → `main`；后续 favicon PR #2-#4 也已明确同意并合并。
- [x] 已审阅 PR checks 并明确作出合并决定；功能分支仍保留，未在收尾中自动删除。
- [x] 当前 GitHub commit/PR 历史可见；仍需本人确认课程提交说明如何概括 AI/subagent 工作与人工修改。
- [ ] 将最终版本推送到课程要求的 NJU Git 仓库。当前已确认的远程仓库是 GitHub；若课程平台另有 NJU Git 地址，需要本人创建或授权后再添加。
- [x] 2026-08-12 的最终生产基线 `2a3cd36` 对应 GitHub Actions [run 31598691090](https://github.com/TheOwl060218/RoamBot/actions/runs/31598691090)，`unit-test` 与 `docker-build` 均成功；`.gitlab-ci.yml` 中存在名为 `unit-test` 的 job。
- [ ] 本人确认公开 GHCR 镜像与课程最终提交所采用的分支/标签一致。
- [ ] 在课程平台提交同一个最终仓库链接、公网 WebUI URL，以及老师要求的其他入口。

## 七、最终提交前逐项勾选

- [x] `SPEC.md`
- [x] `PLAN.md`
- [x] `SPEC_PROCESS.md`
- [x] `AGENT_LOG.md`
- [x] `README.md`
- [x] 学生本人确认的 `REFLECTION.md`
- [x] 完整源代码与测试
- [x] 一键测试命令
- [x] `.gitlab-ci.yml` 的 `unit-test` job
- [x] `Dockerfile`、公开镜像与运行说明
- [x] `.env.example` 仅含非敏感配置
- [x] 最后一次 CI/CD pass 记录
- [x] 可公开访问的 WebUI URL
- [ ] 仓库及 Git 历史中没有真实凭据
