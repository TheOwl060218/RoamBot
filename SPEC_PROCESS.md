# SPEC_PROCESS

## 1. 初始想法

用户最初希望做一个 B 类应用项目：输入出发地址、意愿距离和风景种类，系统结合天气推荐未来可出行地点。

## 2. 关键迭代记录

### 迭代 1：A/B 项目方向选择

- 讨论问题：A 类 Coding Agent Harness 与 B 类应用项目的差异。
- 人工决策：选择 B 类，因为选修课不追求分数高低，用户认为应用类更有意思。
- 结果：项目不实现自主 agent harness。

### 迭代 2：预测时间窗口

- 讨论问题：最初设想是未来三天，后来认为天气预报更远会变不准。
- 人工决策：改为未来一周内，因为一周是天气预报仍有参考价值的合理上限。
- 结果：`SPEC.md` 中约束日期范围不得超过未来 7 天。

### 迭代 3：交通查询范围

- 讨论问题：是否接入 12306 和机票。
- 人工决策：放弃 12306 和机票作为核心功能，避免接口不稳定和合规风险。
- 结果：核心只保留天气、地点、距离、多人汇合与 LLM 解释。

### 迭代 4：朝霞/晚霞预报

- 讨论问题：是否接入 SunsetBot。
- 人工决策：不强接 SunsetBot；朝霞/晚霞作为风景类型，采用本地天气规则评分。
- 结果：降低外部服务依赖风险。

### 迭代 5：多人多地汇合

- 讨论问题：是否允许额外填写 1-2 个地址，支持多人汇合旅游。
- 人工决策：加入该功能，作为项目特色。
- 结果：新增多人可达性评分、公平性评分与解释。

### 迭代 6：核心场景主次

- 讨论问题：RoamBot 应优先服务“个人按天气、距离、风景偏好找地点”，还是“多人约地方出游”。
- 人工决策：个人天气/距离/风景推荐是主线；多人多地汇合是额外功能。
- 结果：SPEC 将多人功能降级为增强场景；推荐指数仍保留多个出发地到目的地的距离方差，用于衡量多人出发是否均衡。

### 迭代 7：地理范围与默认城市

- 讨论问题：RoamBot 应固定城市、支持同城/省内，还是完全由用户输入最大距离决定。
- 人工决策：最终目标是按用户输入的出发地和最大距离决定范围；苏州作为默认演示城市。用户不填写城市时也默认使用苏州。
- 结果：SPEC 增加可选 `city` 输入，缺省为苏州；候选地点模块优先覆盖苏州及周边演示地点。

### 迭代 8：风景类型

- 讨论问题：应保留哪些风景类型，避免范围过大。
- 人工决策：保留湖景、海景、古镇/历史街区、博物馆、公园/绿地/湿地、山地/徒步。
- 结果：SPEC 更新风景类型枚举；苏州默认演示优先覆盖除海景外的类型，海景作为跨城市或较远距离扩展类型保留。

### 迭代 9：排序权重与无结果处理

- 讨论问题：推荐排序应固定权重，还是允许用户自己调整；风景类型和距离应作为评分项还是硬约束；规定范围内无结果时如何展示。
- 人工决策：风景类型匹配与最大距离范围是硬约束；用户可自定义天气、距离、多人距离方差、景区热度/推荐度的权重。默认单人排序为天气 40%、距离 30%、热度 30%；默认多人排序为天气 40%、多人距离方差 40%、热度 20%、距离 0%。
- 结果：SPEC 增加 `RankingWeights` 数据模型、无结果状态与扩展推荐窗口要求。范围内无结果时，常规窗口显示“规定范围内无检索结果”，下方展示最近距离与最热门的符合风景要求景点。权重 UI 使用单条分段滑块自动归一化，单人显示 2 个滑块/3 段，多人显示 3 个滑块/4 段。

### 迭代 10：时间充裕与日期区间天气评价

- 讨论问题：是否仍按两天赶工范围做最小版；用户选择多天日期时，推荐逻辑应取天气最好的一天，还是评价整个日期区间。
- 人工决策：当前作业有约一个月打磨时间，不按两天赶工版收缩范围。RoamBot 应推荐“用户所选 n 天整体都适合”的地点，同时在前端展示该地点在这几天内每天各自的旅游适宜度。
- 结果：SPEC 将天气评估从“最佳日期”改为“每日适宜度序列 + 区间综合天气分”；推荐结果从“最佳日期推荐”改为“日期区间整体推荐”。

### 迭代 11：区间天气聚合方式

- 讨论问题：多日天气适配分应如何聚合，是否要根据用户选择的风景类型解释每天适宜度。
- 人工决策：采用“每日适宜度平均分 + 差天气惩罚”的方案；前端每日适配度展示应告知分数来源。
- 结果：SPEC 增加每日适宜度解释，并明确区间综合天气分不取最佳单日。

### 迭代 12：多风景类型解释模式与指定地点评估

- 讨论问题：用户选择多个风景类型时，是要求尽量全部覆盖，还是匹配任意偏好即可；用户已有目标地点时是否还需要返回推荐地点列表。
- 人工决策：多风景类型解释模式由用户选择，可选“尽量覆盖全部偏好”与“匹配任意偏好即可”。新增指定地点评估模式，用户可直接输入目标地点如“金鸡湖”，系统返回该地点推荐指数而非推荐列表。
- 结果：SPEC 增加 `mode`、`target_place`、`scenery_match_mode` 输入，新增 `PlaceEvaluation` 数据模型和指定地点评估流程。

### 迭代 13：多偏好不做日程安排

- 讨论问题：选择多个风景类型时，RoamBot 是否应该安排每天玩什么。
- 人工决策：不做具体日程安排，避免过度介入用户实际操作；只计算偏好覆盖度并解释覆盖与未覆盖项。
- 结果：SPEC 明确 RoamBot 不输出“第几天玩什么”，只输出地点或指定地点评估结果的风景偏好覆盖说明。

### 迭代 14：指定地点模式的防御性输入

- 讨论问题：用户填写目标地点后，是否还允许填写风景类型。
- 人工决策：不允许。指定地点评估模式下前端隐藏或禁用风景类型输入，后端拒绝“目标地点 + 风景类型”的矛盾请求；地点风景标签由系统识别。
- 结果：SPEC 将 `scenery_types` 和 `scenery_match_mode` 标为仅推荐模式使用，并补充后端校验规则。

### 迭代 15：地图 API 选择与游玩时长边界

- 讨论问题：地图 API 是否能稳定支持地址转坐标、周边景点检索、地点分类、距离/路线，以及是否能直接提供推荐游玩时长。
- 人工决策：主选高德地图 Web 服务 API，覆盖国内地址解析、POI/周边检索、地点分类和路线/距离估算；百度地图作为备选调研对象，本地 mock 数据用于测试与降级。
- 结果：SPEC 将地图服务从“最终实现前确认”改为“主选高德地图 Web 服务 API”。推荐游玩时长不作为地图 API 稳定字段依赖；如需展示，由 RoamBot 根据地点类型、热度/推荐度和规则估算，并标注为估算值。

### 迭代 16：天气 API 选择

- 讨论问题：天气 API 应选高德天气以减少供应商，还是选择更适合逐日天气字段的独立天气服务。
- 人工决策：主选 QWeather（和风天气）每日天气预报 API。虽然供应商支持更长预报，RoamBot 产品层仍默认使用未来 7 天作为可信窗口。
- 结果：SPEC 将天气服务从“优先选择支持未来一周预报的 API”改为“主选 QWeather”；`WeatherDaily` 字段调整为温度、降水量、云量、能见度、风力/风速、湿度和紫外线等可用于旅游适宜度评分的结构化字段。

### 迭代 17：LLM 解释模块与测试成本边界

- 讨论问题：学校平台有 300 元 LLM 额度，是否意味着后续测试会持续消耗 token；B 类应用是否也需要 mock。
- 人工决策：LLM 解释模块采用 OpenAI-compatible provider 抽象，默认演示模型使用学校额度平台的 DeepSeek V4 Flash。真实 LLM 仅在配置 key 后启用；测试与 CI 使用 mock/template，不消耗 token。
- 结果：SPEC 明确 mock 在本项目中是普通外部 API 测试手段，不是 A 类 harness 专属工作；LLM 只负责解释生成，不参与推荐评分、不自主调用工具。

### 迭代 18：多人公平性定义与 API 成本估算

- 讨论问题：多人汇合时“公平”应看距离方差、最远的人，还是二者结合；高德和 QWeather 的真实调用大概会花多少钱。
- 人工决策：多人公平性采用“距离差异/方差”定义，方差越小越公平。平均距离和最大距离保留为解释信息，但不作为公平性主定义。
- 成本判断：测试与 CI 使用 mock，不调用真实 API。真实演示中，高德主要成本来自地理编码、POI 检索和路线/距离调用；QWeather 在低请求量下通常远低于高德成本。后续实现必须限制真实路线/距离和天气调用的候选数量。
- 结果：SPEC 增加 `GroupAccessibilityScore` 数据模型和 API 成本控制边界。

### 迭代 19：结果展示与产品边界

- 讨论问题：结果页面是否加入地图、路线和导航展示。
- 人工决策：不加入地图、路线或导航。具体路线和行程安排不属于 RoamBot 的设计范围，继续扩展会模糊“推荐与解释”的核心职责。
- 结果：WebUI 只使用地点卡片展示距离、每日天气、分项评分、总体推荐度和推荐理由；高德仅作为后台地址解析、地点检索和距离估算的数据服务。

### 迭代 20：Web 主交付与微信小程序扩展

- 讨论问题：B 类项目是否必须选择网页、桌面软件或微信小程序，以及登录、收藏、历史和分享应落在哪个平台。
- 人工决策：正式作业先完成响应式 Web 应用；登录、收藏、历史记录和分享按 Web 功能设计。只有 Web 主项目通过全部验收且仍有时间时，才考虑制作供个人使用的微信小程序。
- 结果：当前 SPEC 和 PLAN 不包含微信小程序实现任务；微信小程序被标记为验收后的独立扩展，避免影响线上 WebUI、测试、CI 和分发等硬性要求。

### 迭代 21：网页登录范围

- 讨论问题：响应式 Web 应用是否需要登录，以及采用本地账户、第三方登录还是更完整的邮件账户体系。
- 人工决策：采用本地用户名和密码账户，支持注册、登录和退出；不做邮箱验证、找回密码或第三方登录。
- 结果：SPEC 增加用户账户模块、`User` 与 `UserSession` 数据模型和密码/会话安全验收标准；账户模块作为收藏和历史记录的前置依赖。

### 迭代 22：收藏保存对象

- 讨论问题：收藏应保存地点本身，还是保存包含旧天气和旧评分的整次推荐结果。
- 人工决策：只收藏地点本身。用户再次打开收藏时，根据新的出发地、日期和当前可用天气重新评估，避免把过期天气当成当前结论。
- 结果：SPEC 增加地点收藏模块和 `UserFavorite` 数据模型；收藏与历史记录分工明确，收藏用于长期地点偏好，不承担结果快照职责。

### 迭代 23：查询历史语义

- 讨论问题：历史记录应只保存查询条件并在打开时重算，还是保留当时的结果，同时提供显式重新查询。
- 人工决策：仅记录成功查询，保存查询输入、生成时间和当时的结果摘要；旧结果明确标为历史快照，不自动更新。重新查询使用当前可用天气计算并新增历史，非法或失败请求不记录。
- 结果：SPEC 增加查询历史模块和 `SearchHistory` 数据模型，并把历史快照与收藏地点、当前重新评估三种语义分开。

### 迭代 24：分享链接与隐私边界

- 讨论问题：分享应使用公开链接、图片还是平台专用接口，以及接收者是否必须登录。
- 人工决策：采用无需登录即可查看的只读分享链接。链接内容来自历史快照，必须隐藏用户身份和详细出发地址；创建者可撤销。不做社交动态、评论或微信专用接口。
- 结果：SPEC 增加只读分享模块和 `ShareLink` 数据模型，并要求不可猜测令牌、令牌哈希存储、公开内容脱敏和撤销后立即失效。

### 迭代 25：SQLite 持久化与 Docker 分发

- 讨论问题：账户、收藏、历史和分享数据使用轻量本地数据库还是独立数据库服务，以及作业是否强制使用 Docker。
- 人工决策：使用 SQLite，并在 Docker 中通过数据卷持久化数据库文件。作业强制要求选择一种分发方式，但不单独强制 Docker；RoamBot 作为带服务端的 Web 应用，正式选择 Docker 容器分发并发布公开镜像。
- 环境检查：本机 Docker CLI 已安装，版本为 29.4.0；当前 Docker Engine 未运行，进入镜像构建与运行验证阶段前需要启动 Docker Desktop。
- 结果：SPEC 增加 SQLite、Docker 数据卷和公开镜像仓库要求；PLAN 在账户模块之前增加持久化任务。

### 迭代 26：历史删除与分享联动

- 讨论问题：包含出发地址的历史记录是否允许用户删除，以及删除后公开分享链接如何处理。
- 人工决策：支持删除单条历史和清空全部历史；删除历史时关联分享链接立即失效，但收藏地点不受影响。当前不做数据导出或账户注销。
- 结果：SPEC 补充历史删除、跨用户隔离和分享级联失效规则；PLAN 增加对应失败测试。

### 迭代 27：游客与登录用户边界

- 讨论问题：使用核心推荐前是否强制注册登录。
- 人工决策：不强制。游客可直接使用推荐模式和指定地点评估；登录只用于收藏、历史和创建/撤销分享链接。游客查询不写入个人历史，公开分享链接仍可匿名查看。
- 结果：SPEC 将登录定义为个人数据功能的边界，而非核心推荐入口；WebUI 和 API 验收增加游客访问测试。

### 迭代 28：加密凭据库与管理员边界

- 讨论问题：作业要求的安全存储是否可仅用 `.env` 满足；忘记主密码后，别人是否能从网页清空并替换 API key。
- 人工决策：`.env` 只用于非敏感配置，真实 API key 保存到带主密码的加密凭据库。初始化、状态、更新、清除和紧急重置只允许本机管理员终端执行，公开 WebUI 不提供凭据写入口。正常清除需要当前主密码；紧急重置需要 Docker 主机权限和不可逆确认，且不删除 SQLite 用户数据。
- 结果：修正 SPEC 原有仅依赖 `.env` 和运行时环境变量的合规缺口，增加独立凭据模块、威胁模型、管理员权限边界和确定性安全测试任务。

### 迭代 29：前后端分离与单镜像部署

- 讨论问题：采用 FastAPI 同时渲染页面的单体结构，还是使用 React/Vue 与 FastAPI 分离；额外技术复杂度是否会影响项目完成。
- 方案比较：单体结构工程量较小；前后端分离更适合当前已扩展出的登录、收藏、历史、分享、双模式表单和动态权重交互；多服务部署则超出需要。
- 人工决策：采用 React + TypeScript + Vite 前端、FastAPI 后端。开发时分别运行，生产时将前端构建产物和后端打入同一个 Docker 镜像并使用同一域名，以避免不必要的跨域认证复杂度。
- 结果：SPEC 更新系统架构、技术栈与测试矩阵；PLAN 分开后端 API 和 React 前端任务，并增加 Vitest 与 Playwright。

### 迭代 30：风景类型识别来源

- 讨论问题：高德返回的地点信息如何转换为 RoamBot 的湖景、海景、古镇/历史街区、博物馆、公园/绿地/湿地和山地/徒步标签。
- 人工决策：使用高德 POI `type` / `typecode` 作为主要依据，以地点名称关键词补充，并允许对少量知名地点配置人工修正。一个地点可以拥有多个标签；无法可靠识别时标记为未知，LLM 不参与分类。
- 结果：SPEC 明确确定性分类流程和独立映射配置；PLAN 为类别映射、关键词、人工修正优先级及未知地点增加测试要求。

### 迭代 31：POI 失败降级与演示数据边界

- 讨论问题：高德 POI 检索失败后使用缓存或内置苏州地点分别意味着什么，是否会误导其他地区用户。
- 人工决策：可使用仍在有效期内的同范围缓存并标注来源。内置苏州地点只允许在管理员显式开启的演示模式中使用，页面必须标注“演示数据”；正式模式无有效缓存时明确报地点服务不可用，不得静默替换真实高德结果。
- 结果：SPEC 明确正式模式、缓存降级和演示模式的边界；PLAN 增加防止演示数据泄漏到正式模式的测试。

### 迭代 32：景区热度分来源

- 讨论问题：高德没有为所有景点稳定提供统一数字热度时，排序中的景区热度/推荐度如何获得。
- 人工决策：根据高德 POI 综合搜索排名生成基础热度分，并允许对少量知名地点使用可审查的本地修正；界面明确标为“RoamBot 热度估算”，不冒充高德官方评分。
- 结果：保留此前确认的默认权重与用户自定义权重；SPEC 和 PLAN 补充热度来源、透明标注及测试要求。

### 迭代 33：多日天气分比例

- 讨论问题：日期区间天气分中的每日平均表现与最差一天分别占多少比例。
- 人工决策：采用 `每日适宜度平均分 × 70% + 最差一天适宜度 × 30%`，结果限制在 0-100 分。
- 结果：将原先“平均分 + 差天气惩罚”的原则细化为可直接实现和测试的确定性公式。

### 迭代 34：WebUI V1 页面基线与迭代方式

- 讨论问题：第一版是否按当前页面结构实现，以及前后端分离后能否根据实际效果继续调整前端。
- 人工决策：首屏直接使用推荐工具；顶部提供推荐、收藏、历史和账户入口；桌面端左右布局、手机端上下布局，结果使用地点卡片。该方案作为 V1 基线，开发服务器运行后可根据实际体验持续修改前端视觉和交互。
- 结果：SPEC 和 PLAN 增加响应式结构、数据来源状态、桌面/手机验证及迭代边界；评分和数据语义仍由后端统一负责。

### 迭代 35：前后端 API 与数据保存边界

- 讨论问题：推荐与指定地点评估是否共用一个接口，以及会话、个人数据和非敏感界面偏好分别保存在哪里。
- 人工决策：公开 JSON API 使用 `/api/v1`；推荐和指定地点评估采用两个独立入口，内部共享领域服务。登录采用服务端会话、`HttpOnly` Cookie 和 CSRF 防护；浏览器只保存非敏感界面偏好，个人数据和外部 API 缓存由 SQLite 保存。
- 结果：SPEC 和 PLAN 增加接口契约、后端资源归属校验、统一错误结构、脱敏分享和会话安全测试。

### 迭代 36：测试交付与真实凭据申请时机

- 讨论问题：高德和 QWeather 凭据何时需要提供，以及真实调用是否进入日常测试和 CI。
- 人工决策：设计、实现和自动测试阶段全部使用 mock；provider 适配器与 mock 测试通过后，用户再申请凭据，并仅通过本机隐藏终端录入。真实 API 冒烟测试独立手动执行，不进入默认测试或 CI。
- 结果：SPEC 与 PLAN 增加凭据时机、人工 smoke task、固定小样本、调用计数和零真实额度 CI 要求。

### 迭代 37：用户故事复核与注册验证边界

- 讨论问题：是否已收集用户故事，以及 V1 是否增加网页验证码、手机号和短信验证。
- 复核结果：SPEC 已包含 13 条正式用户故事，并已进一步拆为模块、验收标准和实施任务。
- 人工决策：V1 继续使用用户名和密码，不收集手机号、不发送短信验证码、不接入网页验证码服务；相关账户增强待 Web 主项目全部验收后再评估。
- 结果：完整设计获得用户批准，进入 `writing-plans` 阶段。

### 迭代 38：可执行计划拆分与批准稿一致性复核

- 讨论问题：完整设计如何拆成可执行的 TDD 任务，同时避免把真实 API、Docker、账户和前端一次性混在同一阶段。
- 人工决策：按依赖顺序拆成四个里程碑：核心后端 mock 垂直切片、账户与 SQLite 数据、React WebUI、真实 provider 与 Docker/GitLab 交付。自动测试和 CI 始终使用 mock，真实凭据继续推迟到独立人工 smoke 任务。
- 复核修正：
  - 将距离 provider 失败恢复为批准稿规定的、明确标注的直线估算，而不是直接终止请求。
  - 将部分天气失败处理为排除缺失天气的候选；全部天气失败终止请求且不记录历史，不通过重新归一化掩盖缺失维度。
  - 将缓存基线统一为地理编码 30 天、POI 7 天、距离 1 天、天气 1 小时，只使用未过期缓存。
  - 统一风景枚举、provider 文件名、调用上限和 `SourceState` 优先级，移除计划中的占位写法。
  - 删除 `.env.example` 和 README 中遗留的明文 API key 配置路径，改为非敏感 mock 配置与加密凭据库 CLI。
- 结果：形成 `docs/superpowers/plans/` 下的一份路线图和四份实施计划，下一步按作业流程进行陌生 agent 冷启动验证。

### 迭代 39：第一次陌生 agent 冷启动失败与根文档回填

- 验证方式：启动一个不继承当前对话的独立 agent，严格只允许读取根 `SPEC.md` 与 `PLAN.md`，不读取 `docs/` 或过程记录，不编辑代码。
- agent 选择：领域层 Task 2.6 天气评分，以及系统层 Task 2.15 FastAPI API。
- 发现的问题：
  - 天气只有 70/30 聚合公式，没有每日温度、降水、风速、能见度和 UV 的确切阈值、单位、缺失值策略与指定地点评估标签语义。
  - “至少匹配一个”与 `cover_all` 的硬过滤、覆盖扣分关系不清楚。
  - 日期七日窗口未明确是否包含今天、是否闭区间和使用哪个时区。
  - 根 PLAN 声称子计划才是执行依据，但冷启动又只提供根文件，导致文件、第一条测试和任务粒度不自包含。
  - 个人数据 API 缺少完整路由、HTTP 状态码、错误码、CSRF 签发/传输、登录用户核心 POST 的 CSRF 语义、降级成功是否写历史、公开分享脱敏和 SPA fallback 规则。
- 修订：
  - 在 SPEC 固定每日天气扣分表、单位、标签保守规则、缺失天气处理、`Asia/Shanghai` 今天至第 6 天闭区间、覆盖惩罚和历史记录语义。
  - 在 SPEC 增加完整 API 路由表、状态码、错误信封与错误码、CSRF 规则、历史事务、分享删除地址和静态 SPA fallback。
  - 统一根 SPEC 数据模型与详细实现计划字段，移除 `trip_weather_score`/`weather_score` 重复语义。
  - 将根 PLAN 重写为四个里程碑、37 个任务的自包含执行入口，每项都有确切文件、第一条失败测试、最小实现和验证命令。
- 结论：第一次冷启动判定“需先修订文档”是有效失败；修订后必须使用另一个全新 agent 再验证，不能直接进入实现。

### 迭代 40：第二次陌生 agent 冷启动失败与外部契约回填

- 验证方式：使用第二个不继承当前对话的独立 agent，只允许读取修订后的根 `SPEC.md` 与 `PLAN.md`，由其选择 M2 认证 API 和 M4 高德适配器做执行前推演。
- agent 发现：
  - 数据库只保存 `csrf_hash`，但文档又要求 `/auth/me` “恢复”CSRF 明文，无法按现有模型实现。
  - 会话期限、Cookie 的 Path/清除属性、注册是否自动登录、认证成功 JSON 与退出响应没有全部锁定。
  - 根 PLAN 的“运行 Ruff”没有给出可复制的确切命令。
  - 高德任务缺少固定主机、必需参数、around/text 选择、关键词、分页、距离单位、provider 成功条件、POI 去重和畸形数据策略；子计划还沿用了旧版 `offset/page` 参数名。
- 修订：
  - 会话固定 24 小时且不滑动；注册自动登录；Cookie 固定 `HttpOnly; SameSite=Lax; Path=/; Max-Age=86400`；认证响应形状和状态码固定。
  - `/auth/me` 改为原子轮换 CSRF：数据库仍只保存哈希，响应只返回一次新明文；前端遇到 `csrf_invalid` 时 single-flight 刷新并至多重试一次。
  - 根 PLAN 固定 Ruff 与完整 backend pytest 命令。
  - 高德契约固定为 `restapi.amap.com` 的 geocode、V5 around/text 和 V3 distance，写清全部参数、六种关键词、50 km 分界、第一页/25 条、GCJ-02、去重/合成 ID、畸形响应和单位转换，并修正为 `page_size/page_num`。
- 结论：第二次冷启动同样判定“需先修订文档”，反馈有效；必须由第三个全新 agent 只读根文档复测通过后才能进入实现。

### 迭代 41：第三次陌生 agent 冷启动失败与评分/表单契约回填

- 验证方式：第三个独立 agent 只读根 `SPEC.md` 与 `PLAN.md`，自行选择 M1.4 评分排序和 M3.3 双模式表单做 TDD 推演。
- agent 发现：
  - 区间天气已有公式，但距离、公平性、provider rank 热度、总分舍入、并列排序和权重非法输入没有在根文档锁定；风景匹配分与覆盖惩罚关系含糊。
  - 根任务没列评分公开函数签名，无法写出第一条可执行 import 测试。
  - 表单缺少 props、默认模式/日期/距离/覆盖、隐藏字段策略、风景选择顺序、权重手柄算法、人数切换行为、客户端校验文案和固定 npm 命令。
  - 根计划的 M1-M4 状态及 M3.3 到 M3.4 的接入边界不够明确。
- 修订：
  - 将详细计划中已有的距离线性分、总体标准差公平性、四项加权和覆盖扣分公式提升到根 SPEC；热度改为不依赖候选总数的 rank 固定公式，V1 本地 bonus 映射为空。
  - 固定两位舍入、并列排序、权重字段/范围/单人公平性限制、评分函数签名与精确测试命令。
  - 固定表单 props、默认 50 km/明天/any、字段文案、隐藏字段省略、选择顺序、5% 相邻权重分配、人数变化 reset、错误文案和 npm scripts。
  - 明确 37 个实施任务均待实施，M3.3 只产出表单，M3.4 的 `SearchWorkspace` 负责接入 API。
- 结论：第三次冷启动判定“需先修订文档”有效；必须再由第四个全新 agent 只读根文档复测。

### 迭代 42：第四次陌生 agent 冷启动失败与模型/QWeather 契约回填

- 验证方式：第四个独立 agent 避开已审任务，只读根文档并选择 M1.2 领域模型与 M4.4 QWeather 适配器推演。
- agent 发现：
  - `DailyWeather/WeatherDaily`、`RecommendationItem/Recommendation`、`PlaceEvaluationResponse/PlaceEvaluation` 命名不一致，模型字段类型、枚举 JSON 值、null/空白/额外字段、weights 缺省与校验错误断言未锁定。
  - M1.2 未明确要创建所有被响应引用的领域类型。
  - QWeather 缺类/方法签名、fixture 文件名、账户 Host 规则、请求 query/header、坐标处理、成功 code、日夜字段选择、缺日/重复/乱序/畸形行为和稳定错误码。
- 修订：
  - 根 SPEC 新增完整 Pydantic v2 领域/API 模型契约，统一英文枚举、类型名、字段默认/可空性和 extra-forbid；补 `GroupAccessibilityScore` 到返回项。
  - M1.2 固定全部公开类型和第一条可执行 `RecommendationRequest` 测试。
  - 核对 QWeather 当前官方每日预报、API Host 与 API KEY Header 文档；V1 继续使用官方支持的 `X-QW-Api-Key`，固定 Host、query、两位经纬度、GCJ-02 原值、必需 daily 字段、日夜合并、错误码和 `weather_7d_success.json`。
  - 固定 `ProviderHttpClient.get_json`、`ProviderBudget.consume` 与 `QWeatherProvider.daily` 签名。
- 结论：第四次冷启动判定“需先修订文档”有效；必须由第五个全新 agent 只读根文档继续抽查。

### 迭代 43：第五次陌生 agent 冷启动失败与项目骨架边界回填

- 验证方式：第五个独立 agent 排除前四轮任务，只读根文档并选择 M1.1 FastAPI 健康入口与 M3.1 Vite 应用壳。
- agent 发现：
  - 根计划未固定 app factory 导出名，也未给出从 Python 3.13 创建 `.venv`、安装 editable backend 的命令，第一条红测无法直接运行。
  - M1.1 文件清单缺 Python 包 `__init__.py`。
  - M3.1 文件清单缺 Vite HTML/React 入口、TS/ESLint 配置、测试 setup；同时它要求“可操作工作区”，但表单属于 M3.3，任务边界冲突。
- 修订：
  - 固定 `create_app()`、模块级 `app`、`roambot.main:app`、完整 package 文件、pyproject 依赖和 venv/install/red-green 命令。
  - 固定 Vite scaffold 的完整文件清单、package scripts、AppShell props、placeholder routes、导航/账户可访问名称和构建命令。
  - M3.1 只验证壳能渲染注入内容，不提前断言表单；M3.3/M3.4 完成后再按里程碑验证最终首屏可操作。
- 结论：第五次冷启动判定“需先修订文档”有效；必须由第六个全新 agent 继续抽查未覆盖任务。

### 迭代 44：第六次陌生 agent 冷启动失败与分享/Docker 契约回填

- 验证方式：第六个独立 agent 只读根文档，优先抽查个人数据和交付，选择 M2.7 匿名分享与 M4.8 单 Docker 镜像。
- agent 发现：
  - 分享创建响应、公开 snapshot 层级、重复创建语义和仓储/router 文件边界未定义；只存 token 哈希又无法复用旧明文链接。
  - Docker 静态测试未命名且依赖尚未构建的 `frontend/dist`；app factory 无临时目录注入；entrypoint 的模式/secret/退出行为及 Compose 服务、端口、volume 和 smoke 命令未锁定。
- 修订：
  - 每次创建分享固定撤销旧活动链接并返回新的 201 相对 URL；固定私有 create JSON、公开 snapshot JSON 和严格脱敏 item 字段，补仓储/服务/main 文件。
  - `RecommendationItem` 补原始 `weather` 列表，供结果和分享展示而不要求前端猜测。
  - 固定 `create_app(frontend_dist=None)`、临时静态 fixture 测试、`test_static_serving.py`/`test_entrypoint.py`、exit 78、secret path/env、0.0.0.0:8000、service/volume 名和完整 docker smoke。
- 结论：第六次冷启动判定“需先修订文档”有效；必须由第七个全新 agent 继续抽查。

### 迭代 45：第七次冷启动失败与数据库/缓存契约回填

- 验证方式：第七个全新 agent 只读根文档，选择 M2.1 SQLite/Alembic 与 M4.5 新鲜缓存/降级做 TDD 推演。
- agent 发现：Alembic 框架表与七张业务表边界、迁移文件/Schema/UTC/FK/临时 URL 不完整；缓存 TTL 截止点、key、候选五项顺序、天气/距离失败和 notices 触发不足。
- 修订：固定业务表/框架表、完整持久化类型与约束、Alembic URL 注入；固定 cache-first、不用过期缓存、五候选不回填、距离估算、天气排除/全失败和来源文案。
- 结论：有效失败；M2.1 经下一轮复核通过。

### 迭代 46：第八/九次定点复核与最终通过

- 第八次复核：确认 M2.1 可执行；M4.5 仍缺具体 cache params/payload 和 notice 事件范围。
- 第九次复核首次结果：确认大部分契约已明确，但缓存仓储/装饰器/clock、POI resolve 单对象、trace 类型和超距后天气调用仍需写死。
- 最终修订：固定 `CacheEntry/CacheRepository`、五种 key/payload 场景、四个缓存装饰器签名、每请求 `ProviderTrace`、`ProviderBundle/Runtime`，以及“距离→硬过滤→天气”的零无效调用顺序。
- 定点复测：第九个审查者重新读取根文档后返回“通过，可进入实现”。
- 结果：冷启动门槛满足；规划阶段结束，不再增加产品范围。

## 3. Superpowers 复核状态

- `brainstorming`：已完成分段确认，形成 `docs/plans/2026-07-15-roambot-design.md`，并由用户批准。
- `writing-plans`：已完成一份路线图和四份可执行计划；已通过占位扫描、接口名称检查、批准稿规则对照和 `git diff --check`。
- `cold-start`：九轮隔离验证及定点复测完成；最后结论为“通过，可进入实现”。

## 4. 冷启动验证待办

规划验证已完成。下一步按 `docs/superpowers/plans/2026-07-15-roambot-roadmap.md` 从核心后端开始，使用 `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans`，每项遵循红-绿-重构和独立复核；真实 API key 继续推迟到 M4.7 人工 smoke。
