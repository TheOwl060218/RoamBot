# SPEC: RoamBot

## 1. 问题陈述

短途出游常见困难不是“没有地方去”，而是用户很难同时判断未来天气、可接受距离和想看的风景类型。RoamBot 首先服务个人用户：当用户提出天气、距离和风景偏好要求后，程序帮助找到合适地点并给出推荐选项。多人多地汇合是额外功能，用于在有同行者时把目的地点到多个出发地的距离方差纳入推荐指数。

本项目构建一个 B 类应用项目：**RoamBot，一个天气感知的风景出行推荐助手**。用户输入主出发地址、最远可接受距离、风景类型与可出行日期范围；系统结合地点候选、天气预报和距离评分，输出按推荐度排序的地点与自然语言解释。用户也可以额外输入 1-2 个同行人地址，系统会将多个出发地到候选目的地的距离方差作为额外评分因素。

本项目不是自主 agent。系统按固定流程调用地图、天气和 LLM；LLM 只负责基于结构化推荐结果生成解释文本，不自主选择工具、不循环决策、不执行外部动作。因此本项目适用 B 类应用要求，不触发 A 类 Coding Agent Harness 的额外要求。

## 2. 目标用户

- 想在未来一周内安排短途风景出游的个人用户。
- 需要协调 2-3 个不同出发地的同行小组；这是增强场景，不是第一主线。
- 想根据天气决定是否看湖景、山景、城市夜景、朝霞/晚霞、公园等风景的人。

## 3. 用户故事

1. 作为个人用户，我希望输入出发地址和最远距离，以便系统推荐不超出可接受范围的风景地点。
2. 作为结伴出游组织者，我希望额外输入 1-2 个同行人地址，以便系统推荐对大家都相对公平的汇合地点。
3. 作为想看特定风景的人，我希望选择湖景、山景、城市夜景、朝霞/晚霞、公园等类型，以便推荐结果符合我的偏好。
4. 作为担心天气影响出行的人，我希望系统查看未来一周天气，以便避开降雨、高温、低能见度等不适合出游的日期。
5. 作为决策者，我希望看到推荐理由、天气原因、距离原因和公平性说明，以便判断是否采纳推荐。
6. 作为新用户，我希望系统在外部 API key 缺失时给出明确提示，而不是直接崩溃。

## 4. 功能规约

### 4.1 输入与校验模块

输入：
- 主出发地址，必填。
- 额外出发地址，0-2 个。
- 最远可接受距离，单位 km。
- 风景类型，可多选。
- 未来一周内可出行日期范围。

行为：
- 校验地址非空、距离为正数、日期不超过未来 7 天。
- 限制总出发地址数量为 1-3 个。

输出：
- 结构化请求对象，或明确错误信息。

### 4.2 地址解析模块

输入：
- 地址文本列表。

行为：
- 通过地图服务将地址解析为经纬度。
- 测试环境使用 mock 地理编码器。

输出：
- `Origin` 列表：标签、原始地址、经纬度。

错误处理：
- 单个地址无法解析时，返回可读错误，要求用户修改地址。

### 4.3 候选地点模块

输入：
- 主出发点经纬度。
- 风景类型。
- 最远距离。

行为：
- 从地图 API 或本地候选数据集中获取地点。
- 过滤明显超出最远距离的地点。
- 给地点打基础风景匹配分。

输出：
- 候选 `Destination` 列表。

### 4.4 天气评估模块

输入：
- 候选地点经纬度。
- 可出行日期范围。

行为：
- 查询未来一周天气。
- 计算每个地点在每个候选日期的天气适宜度。
- 对朝霞/晚霞类型，额外考虑云量、降雨、能见度、日出/日落时段。

输出：
- 每个地点的最佳日期与天气分。

### 4.5 多人汇合评分模块

输入：
- 1-3 个出发点。
- 候选地点。

行为：
- 估算每个出发点到候选地点的距离或耗时。
- 计算平均距离、最大距离、距离差异。
- 距离越短、差异越小，公平性越高。

输出：
- `GroupAccessibilityScore`。

### 4.6 推荐排序模块

输入：
- 候选地点。
- 天气分。
- 风景匹配分。
- 多人可达性分。
- 公平性分。

行为：
- 计算综合推荐分。
- 按分数降序输出。

输出：
- 推荐结果列表，包含地点、最佳日期、天气摘要、距离信息、推荐分。

### 4.7 LLM 解释模块

输入：
- 结构化推荐结果。

行为：
- 调用 LLM 生成简短、可读、可比较的推荐理由。
- 测试环境使用 mock LLM。

输出：
- 每个推荐地点的自然语言理由与注意事项。

### 4.8 WebUI 模块

输入：
- 用户表单。

行为：
- 展示输入区、推荐结果卡片、配置缺失提示。

输出：
- 可访问 Web 页面。

## 5. 非功能需求

- 性能：一次推荐请求在正常外部 API 响应下应在 10 秒内返回。
- 可用性：外部 API 失败时应返回降级提示，不显示堆栈错误。
- 可测试性：评分、排序、输入校验、LLM 解释封装必须可用 mock 测试。
- 可观测性：后端记录请求阶段、外部 API 成败、降级原因；不得记录真实 API key。
- 安全：凭据不得硬编码、不得提交 Git、不得输出到日志。

## 6. 凭据威胁模型与对策

凭据包括地图 API key、天气 API key、LLM API key。

威胁：
- key 被硬编码进源码。
- `.env` 被误提交。
- 日志或错误信息泄露 key。
- Docker 镜像构建时把 key 打进镜像。

对策：
- 使用 `.env` 本地加载，`.env` 加入 `.gitignore`。
- 提供 `.env.example` 只列变量名，不含真实值。
- Docker 运行时通过环境变量注入 key。
- 配置状态接口只显示“已配置/未配置”，不回显明文。
- README 明确 `.env` 是明文文件，有本机泄露风险。

## 7. 系统架构

前端 WebUI 调用后端 API。

后端按固定流程执行：
1. 校验输入。
2. 解析出发地址。
3. 获取候选地点。
4. 获取未来一周天气。
5. 计算天气分、风景分、距离分；如有额外出发地，再计算多人距离方差分。
6. 排序。
7. 调用 LLM 生成解释。
8. 返回结果给 WebUI。

外部依赖：
- 地图服务：高德地图或腾讯地图，最终实现前确认。
- 天气服务：优先选择支持未来一周预报的 API。
- LLM 服务：可选 OpenAI 兼容接口或其他供应商。

## 8. 数据模型

- `TravelRequest`
  - `primary_origin`
  - `extra_origins`
  - `max_distance_km`
  - `scenery_types`
  - `date_range`

- `Origin`
  - `label`
  - `address`
  - `latitude`
  - `longitude`

- `Destination`
  - `id`
  - `name`
  - `latitude`
  - `longitude`
  - `scenery_tags`

- `WeatherDaily`
  - `date`
  - `condition`
  - `temperature_min`
  - `temperature_max`
  - `precipitation_probability`
  - `cloud_cover`
  - `visibility`

- `TravelEstimate`
  - `origin_label`
  - `destination_id`
  - `distance_km`
  - `duration_minutes`

- `Recommendation`
  - `destination`
  - `best_date`
  - `weather_score`
  - `scenery_score`
  - `accessibility_score`
  - `distance_variance_score`
  - `total_score`
  - `reason`

## 9. 技术选型

暂定技术栈：
- 后端：Python + FastAPI。
- 前端：简洁 WebUI，可由后端静态托管。
- 测试：pytest。
- 分发：Docker。
- CI：GitLab CI，包含 `unit-test` job。

理由：
- Python 适合快速实现推荐评分、API 适配与测试。
- FastAPI 上手成本低，便于提供 Web API。
- Docker 分发最符合“全新机器从零运行”的作业要求。

## 10. 验收标准

- 用户可在 WebUI 输入 1-3 个出发地址、距离、风景类型、日期范围。
- 系统返回至少 3 个按推荐度排序的地点，或说明候选不足原因。
- 每个推荐结果包含最佳日期、天气摘要、距离/公平性说明、推荐理由。
- 输入非法时返回明确错误。
- 外部 API 在测试中可被 mock。
- `make test` 或等价命令可一键运行核心测试。
- Docker 可构建并运行。
- `.gitlab-ci.yml` 包含 `unit-test` job。

## 11. 风险与未决问题

- 地图 API、天气 API 的免费额度与申请流程需要确认。
- 不同 API 的经纬度坐标系可能不同，需要统一处理。
- 真实距离/耗时查询可能产生较多 API 调用，需做数量限制。
- LLM 输出可能不稳定，测试只验证输入输出封装和 mock 行为，不依赖真实 LLM。
- 两天内不接入 12306、机票和 SunsetBot；朝霞/晚霞只做本地规则评分。
