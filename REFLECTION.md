# RoamBot 项目反思

## 一、从真实需求出发

RoamBot 源于一次真实的出行需求：我和哥哥分别从不同地点出发，希望找到一个双方都合适的共同出游地点。现实中，我们需要在地图、天气和搜索平台之间切换，再综合双方路程、天气、地点特点和出行偏好作出判断。这个过程让我意识到，真正需要解决的问题不是“附近有什么地方”，而是“我们在哪里见面比较合适”。因此，RoamBot 的核心定位是面向多人异地出行场景的地点决策助手。

在以前的软件工程项目中，我同样参与过需求分析和系统设计，不过技术实现主要偏后端。RoamBot 与过去最大的不同，是 AI 深度参与了需求讨论、原型实现和验证。开发不再只是按照“分析—设计—编码—测试”的顺序单向推进，而是在 Brainstorming、快速原型、实际体验和需求修正之间循环。AI 加快了实现，更降低了尝试和验证想法的成本。

## 二、Brainstorming 带来的探索与取舍

在 Brainstorming 中，Codex 并不是替我决定需求，而是通过追问、方案比较和复杂度分析提供新的角度，我也会在交流和体验原型时形成新想法。最有代表性的例子是“指定地点评估”。RoamBot 最初主要帮助用户寻找地点，但讨论中发现，用户有时已有目标，只是不确定它当天是否适合前往。由于地点搜索、天气查询、距离计算、评分和推荐理由都能复用，这项功能可以用可控成本扩展原有场景，因此值得加入。

对恶劣天气的处理也体现了讨论的价值。天气不好并不意味着必须删除所有户外地点：江南烟雨、湿地或自然景观有时会因天气形成独特体验，完全过滤还可能使候选过少。因此应结合天气、地点类型和用户偏好调整评分并提示风险，而不是用机械规则替用户决定。未来还可加入“偏好晴天”“接受小雨”等天气偏好。

Brainstorming 也帮助我及时放弃不合适的方案。我曾考虑加入地点图片，技术上可行，复杂度也能接受，但在方案评估中发现图片的比例、色调和构图难以统一；继续改善又会依赖人工筛选和额外处理，与自动化目标不符。因此，我最终没有保留它。这让我认识到，探索的成果不一定是新增功能；低成本证明一个想法不值得继续，同样是有效的产品决策。

## 三、前端原型与人工验收

这是我第一次深入参与前端设计。开始时，我没有预先给出完整界面，而是让 Codex 根据需求生成可操作的功能原型。实际使用后，我记录布局、信息层级、交互和视觉问题，再让 Codex 汇总方案，经我审核后集中改版。初版采用左侧填写条件、右侧展示结果的布局，但两侧内容量差异很大。后来改为上方输入、下方结果，并在生成推荐后收缩输入区，既保留修改入口，也让结果成为重点。

这一过程也让我看到，AI 能执行文字要求，却未必理解没有写出的体验目标。例如，我要求“优化手机端推荐栏”时没说明桌面端保持不变，结果桌面布局也受到影响；我要求推荐后“自动向下滑动”时，Codex 滚到推荐栏，而我真正希望用户先看到收缩后的查询摘要。这些误解不是单纯的编码错误，而是需求没有说明范围和最终状态。到项目后期，我会主动说明改动作用于哪个端、哪些部分保持不变，以及操作结束后用户应该看到什么。人工验收正是把隐含目标补充进下一轮开发。

## 四、对 Superpowers 工作流的反思

Superpowers 中最值得保留的是 Brainstorming，因为它能帮助发现遗漏、比较成本并明确边界。不过，我前期也曾过于机械地执行流程：许多小任务都启动 subagent，再经历审查和测试。严格流程能降低遗漏风险，但普通小改动也会产生上下文切换、等待和 token 消耗，流程成本有时超过任务本身。

项目先后进行了九轮陌生 agent 冷启动。前几轮确实暴露了 SPEC 和 PLAN 中没有说清的隐含假设，例如“七张业务表”没有说明 Alembic 自带的版本表，缓存键、到期边界和降级顺序也不够明确。这证明让不继承上下文的 agent 只读文档推演，可以检验规约是否自包含。但随着问题越来越细，后续复核的收益开始下降，也让我看到严格流程同样需要停止条件。

后期我更倾向于先完成 Brainstorming 和模块计划，由主 agent 连续实现，再进行阶段性审查和全量测试。未来我会按风险分级：核心业务和高风险改动保持严格设计与验证；普通 UI 调整采用轻量流程，在里程碑处集中审查。工程纪律的价值在于控制风险，而不是让每个任务都经历相同步骤。

## 五、测试与个人不足

项目进行了后端、前端和端到端流程测试。测试既减少了后续修改造成的回归，也让验证更加系统。它的价值不只是最后显示“通过”，而是把核心行为变成可重复检查的约束，使快速迭代不再完全依赖人工记忆和临时体验。

一个具体例子是最大距离输入框。我在手工测试中发现，清空数值后页面会立即回填 `0`，继续输入 `200` 时就可能显示成 `0200`。修复时先用回归测试固定这个问题，再让输入框保留字符串编辑状态，只在提交时转换成数值。这个过程让我更直观地理解了测试的作用：它不仅证明修复当时有效，也防止相同交互在后续改版中再次出现。

但我对自己的测试参与仍不满意。许多测试由 Codex 编写或执行，我没有逐一深入阅读，对部分 CI 失败原因也理解不足。今后我会重点阅读核心业务测试和关键端到端用例，亲自分析失败原因，而不是只接受“测试已经通过”。只有理解测试所保护的行为，我才能真正承担质量责任。

## 六、从本地通过到公网可用

本地测试通过后，Railway 公网部署仍暴露了不少回归问题。最让我意识到“代码写完不等于项目完成”的是外部服务降级：线上结果同时受到实际部署版本、网络环境和第三方服务状态影响，问题定位、修改和再次验证都比本地复杂不少。项目还需要处理 Docker 构建、持久化数据卷、最终 CI 和公网回归，这些都不属于功能代码本身，却直接决定网站能否真正交付给别人使用。

这段经历改变了我对完成标准的理解。本地运行和自动测试通过只能说明代码具备交付条件；只有确认部署版本一致、核心流程在真实环境可用，并且外部服务失败时仍能合理降级，项目才算完成了从“实现”到“可用”的转变。

## 七、个人成长与结论

这个项目中，我最明显的成长是需求表达和人工验收能力。从只说明“要做什么”，到主动界定修改范围、保持不变的部分和用户最终看到的状态，我认识到，清晰需求不是写更多文字，而是把影响结果的判断说清楚。原型也不是需求讨论的终点，而是发现问题和继续判断的依据。

RoamBot 让我认为，AI 没有替代开发者，而是改变了开发者工作的重点。实现和验证成本降低后，开发者能探索更多可能，也更需要对项目方向、需求边界、方案取舍和最终质量负责。AI 可以快速生成方案，却不能保证方向正确，也无法代替真实体验后的判断。未来我会继续发挥它在讨论、原型和验证上的优势，同时避免机械套用流程，加强自己对核心测试和失败原因的理解。有效的人机协作不是把责任交给 AI，而是让 AI 扩大探索能力，再由人完成选择、反馈和验收。

## AI 辅助声明

本文中的项目经历、案例和反思观点来自本人提供的材料，Codex 参与了结构整理与文字改写；在 RoamBot 开发过程中，Codex 与 Superpowers 也参与了需求讨论、方案分析、代码实现和测试验证。
