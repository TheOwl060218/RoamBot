# RoamBot 项目反思

> **待学生本人完成。** 作业明确禁止 AI 代写反思报告。下面仅是写作提纲和可核对的项目证据，不是可直接提交的反思正文。请用自己的判断、经历与措辞写成 1500–2500 字，并删除所有提示语。可以请 AI 在完成后只做语病与结构润色，并在文末如实标注辅助范围。

## 1. 项目与个人职责（建议 100–150 字）

- 用自己的话说明 RoamBot 解决什么问题，以及你为何选择这个方向。
- 说明个人项目中你承担的 PM、架构、验收与最终责任。

## 2. 最有价值的 Superpowers 技能（建议 200–300 字）

- `brainstorming`、`writing-plans`、TDD、verification-before-completion 中哪些真正改变了结果？
- 可引用的事实：需求从交通票务收缩为天气感知地点评估；地图、导航和行程安排被明确排除；凭据与 Docker 在实现前写入规约。
- 不要只描述技能用途，要写你自己的判断和一次具体决策。

## 3. 形式大于实质的部分（建议 200–300 字）

- 回顾前期过多陌生 agent 与逐 task 审查为何拖慢进度，你如何要求改成“大里程碑集中审查”。
- 思考作业要求的细粒度 task、每个 worktree/PR 与单人项目实际效率之间的矛盾。
- 写清哪些纪律仍值得保留，避免得出“审查完全没用”的简单结论。

## 4. TDD 是阻碍还是放大器（建议 200–300 字）

- 至少分析一个 RED→GREEN 案例。
- 可引用的事实：最大距离清空会立即回填 `0`，回归测试先复现收到值为 `0`，再改为字符串编辑态；Linux CI 还发现 SQLite 连接释放与 `0600` 权限问题。
- 写出哪些测试值得先写、哪些视觉调整不适合机械追求最细测试。

## 5. SPEC / PLAN 与陌生 agent（建议 200–300 字）

- 从 `SPEC_PROCESS.md` 选择一轮真正暴露隐含假设的冷启动失败。
- 说明问题究竟来自规约不清、计划接口不完整，还是 agent 理解偏差。
- 评价“至少一次冷启动”与本项目实际进行了多轮之间的收益递减。

## 6. Prompt 与上下文策略（建议 150–250 字）

- 说明你后来要求及时收缩上下文、减少重复审查、先做通再调整前端美观的原因。
- 写清什么信息必须持续保留：作业边界、已确认决策、测试证据、真实/Mock 区分、未完成外部事项。

## 7. 凭据、成本与分发（建议 250–350 字）

- 说明为什么不用 `.env` 保存真实 key，而采用主密码加密凭据库、隐藏 CLI 与 secret 文件。
- 讨论“忘记主密码只能 reset”带来的可恢复性与安全性权衡。
- 说明 Mock、缓存、调用预算、无自动重试和最多 5 次人工 smoke 如何控制费用。
- 写 Docker 单镜像和数据卷让你补想了哪些原本容易遗漏的交付问题。

## 8. 如果重做与方法论批判（建议 250–350 字）

- 明确列出 2–3 个会改变的做法，例如更早锁定最小审查频率、先建立 CI、减少计划重复层级。
- 回答 Superpowers 假设了什么：足够时间、廉价 subagent、清晰 task 边界、稳定工具环境等；这些假设在本项目中哪些成立、哪些不成立？
- 给出你认为更适合个人课程项目的改进流程。

## 9. AI 辅助声明

请由学生本人填写实际情况，例如：使用 Codex 与 Superpowers 协助需求澄清、计划、编码、测试和文档事实整理；反思观点与正文由本人完成；AI 仅在最后进行何种程度的润色。
